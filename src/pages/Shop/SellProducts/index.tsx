import {SFC} from 'types';
import * as S from './Styles';

const SellProducts: SFC = ({className}) => {
  return (
    <S.Container className={className}>
      <div>SellProducts</div>
    </S.Container>
  );
};

export default SellProducts;
