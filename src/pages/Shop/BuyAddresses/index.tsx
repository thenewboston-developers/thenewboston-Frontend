import {SFC} from 'types';
import * as S from './Styles';

const BuyAddresses: SFC = ({className}) => {
  return (
    <S.Container className={className}>
      <div>BuyAddresses</div>
    </S.Container>
  );
};

export default BuyAddresses;
